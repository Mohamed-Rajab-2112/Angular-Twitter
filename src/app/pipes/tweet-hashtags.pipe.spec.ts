import { TweetHashtagsPipe } from './tweet-hashtags.pipe';

describe('TweetHashtagsPipe', () => {
  it('create an instance', () => {
    const pipe = new TweetHashtagsPipe();
    expect(pipe).toBeTruthy();
  });
});
